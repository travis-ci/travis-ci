<div id="jobs" class="list">
  <h4>Queue: {{queue}}</h4>
  {{#collection tagName="ul" itemClass="job" contentBinding="jobs"}}
    {{content.repository.slug}}
    {{#if content.number}}
      #{{content.number}}
    {{/if}}
  {{else}}
    No jobs
  {{/collection}}
</div>

